# Phase 2 overlay — apply order

This zip is an **overlay** intended to be applied **after Phase 1** (text-only PWA) is implemented.

Apply order:
1) Implement Phase 1 using `docs/PHASE_EXECUTION_PROMPT.md`
2) Overlay this Phase 2 zip (adds `apps/api` and upgrades `apps/web` for recording + transcription)
3) Run Phase 2 using `docs/PHASE2_EXECUTION_PROMPT.md`

If your Phase 1 file paths differ, integrate manually using the notes in `docs/phase2_patch_notes.md`.
